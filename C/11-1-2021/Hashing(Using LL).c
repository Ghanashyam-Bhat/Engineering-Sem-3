//Hashing

//In hashisng we convert the key value into index usignthe hashing functiom
//The hashing function should be in such a way that it should ensure least number of collision

//The hash table must be made to store pointer to a linked list(The LL must store the info coming under same index due to collision)
